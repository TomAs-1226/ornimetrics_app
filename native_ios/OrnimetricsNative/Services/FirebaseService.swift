import Foundation

struct FirebaseService {
    func configure(with config: AppConfig) {
        // TODO: Initialize Firebase SDK with config values.
        // This keeps backend parity with the existing Firebase project.
    }
}
